# Vendored, read-only copy of article 1's AIPlacementDecision CRD definition,
# fetched from github.com/imperium/ai-sovereign-finops-operator (branch main,
# path operateur/config/crd/bases/aiops.imperium.io_aiplacementdecisions.yaml)
# on 2026-08-01. NOT owned or maintained by this repo — article 1 is the
# source of truth. Applied here only so this repo's own kind cluster can host
# real AIPlacementDecision test objects for functional/attack-scenario
# testing of the Runtime Guard Operator (Phase 5), without deploying all of
# article 1's operator/scheduler (out of scope for this repo).
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: aiplacementdecisions.aiops.imperium.io
spec:
  group: aiops.imperium.io
  names:
    kind: AIPlacementDecision
    listKind: AIPlacementDecisionList
    plural: aiplacementdecisions
    shortNames:
    - apd
    singular: aiplacementdecision
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              evidenceRef:
                description: EvidenceRef references the evidence used for placement.
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the referenced object. Defaults to the
                      owner namespace.
                    type: string
                required:
                - name
                type: object
              placementToken:
                description: PlacementToken controls token minting.
                properties:
                  required:
                    description: Required means the scheduler/controller must mint
                      a verifiable placement token.
                    type: boolean
                  ttlSeconds:
                    description: TTLSeconds limits token validity.
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              policyRef:
                description: PolicyRef references the confidentiality policy used
                  for the decision.
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the referenced object. Defaults to the
                      owner namespace.
                    type: string
                required:
                - name
                type: object
              schedulerName:
                description: SchedulerName is the scheduler expected to honor the
                  decision.
                type: string
              targetRef:
                description: TargetRef identifies the workload or pod being placed.
                properties:
                  name:
                    description: Name of the referenced object.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the referenced object. Defaults to the
                      owner namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - policyRef
            - targetRef
            type: object
          status:
            properties:
              conditions:
                description: Conditions captures placement state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              decision:
                description: Decision is allow, deny or pending.
                type: string
              nodeName:
                description: NodeName is the chosen node when known.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation observed by
                  a controller.
                format: int64
                type: integer
              placementTokenDigest:
                description: PlacementTokenDigest is a digest of the minted placement
                  token.
                type: string
              simulated:
                description: Simulated indicates a kind-local simulated path.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
